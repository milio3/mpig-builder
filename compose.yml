services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mpig-builder
    restart: unless-stopped
    ports:
      - "8000:80"
    environment:
      - TZ=Europe/Madrid
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:80/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
